2.	Задача на понимание многофайловых сборок и подписание сборок, в качестве классов можно выбрать все, что придет в голову) 

-В виду того,что в IDE-среде Visual Studio никакого отдельного шаблона проекта для создания многофайловой сборки на C#, буду пользоваться командной строкой.
-Создаю  .NET-модуль используя комманду csc.exe и опцию /t:module;
-Создаю главный модуль (уже dll)с использованием соответствующих опций /t:library и /out: и флаг /addmodule чтобы включить инфу о двоичном коде первого модуля в манифест сборки. Так же, выбрав из четырёх способов подписи строгим именеи(студией, al.exe, атрибуты сборки, параметры компилятора) подпись с использование компилятора и параметра /keyfile, предварительно сгенерировав пару ключей.

^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

3.	Реализовать методы быстрой сортировки и сортировки слиянием для целочисленного массива.  Протестировать работу методов в консольном приложении или с помощью модульного тестирования. (залить на github).

-Реализую два способа сортировки целочисленного массива (быстрая сортировка и сорт. слиянием) с входными параметрами самого массива и диапозона старта и конца сортировки(можно было сделать и перегруженные). Создаю доп проект для unit тестов и тестирую каждый метод в отдельности с разными массивами.
